---
dependencies:
  post:
     - sudo pip install awscli

test:
  post:
    - go build -o envexpvar:
        pwd: bin/example

deployment:
    testing:
      branch: master
      commands:
        - aws s3 cp bin/example/envexpvar s3://$BUCKET/envexpvar/$CIRCLE_SHA1/envexpvar
